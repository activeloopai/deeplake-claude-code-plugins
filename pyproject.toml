[project]
name = "hivemind-harrier-backfill"
version = "0.1.0"
description = "Local Harrier embedding backfill helpers for Deeplake tables"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "safetensors>=0.4",
  "torch>=2.4",
  "transformers>=4.57",
]

[tool.uv]
package = false
